> Built with AI and I dont give a fuck about it. Just use,

# Zvuk.com Downloader

> **⚠️ ОТКАЗ ОТ ОТВЕТСТВЕННОСТИ:** Данный инструмент разработан исключительно в ознакомительных и учебных целях. Автор не поощряет нарушение условий использования сервиса и не несет ответственности за любой возможный ущерб, блокировку аккаунтов или нарушение авторских прав, возникшее в результате использования этого скрипта. Используйте на свой страх и риск.

Скрипт на Python для автоматизированного скачивания музыки из аудиосервиса **«Звук» (zvuk.com)**. Поддерживает скачивание личных плейлистов, коллекции «Избранное» и массовую загрузку всей медиатеки.

## ✨ Особенности
- **Скачивание в один клик:** возможность выкачать всю медиатеку командой `all`.
- **Умная пагинация:** корректно обрабатывает плейлисты любого размера.
- **Организация файлов:** создает папки с названиями плейлистов и именует файлы `Артист - Название.mp3`.
- **Имитация поведения человека:** случайные задержки, динамические Referer и поддержка HTTP/2.

## 🚀 Быстрый старт

### 1. Подготовка
Убедитесь, что у вас установлен Python 3.8+ и библиотека `httpx` с поддержкой HTTP/2:
```bash
pip install httpx[http2]
```

### 2. Настройка конфигурации (Важно)
Для успешного прохождения проверки антифрод-системы (WAF), данные в скрипте должны совпадать с данными вашего браузера.

1.  **Настройка Cookies:**
    - Откройте [zvuk.com](https://zvuk.com) и войдите в аккаунт.
    - Нажмите `F12` -> вкладка **Network** (Сеть).
    - Обновите страницу и выберите любой запрос к домену `zvuk.com`.
    - В разделе **Headers** (Заголовки) скопируйте значения из поля **Cookie** и заполните словарь `COOKIES_DICT` в скрипте.
    - *Убедитесь, что параметр `wafv` имеет значение `human`.*

2.  **Настройка User-Agent:**
    - В той же вкладке **Network** (Сеть) найдите заголовок **User-Agent**.
    - Скопируйте его полностью (например: `Mozilla/5.0 ... Firefox/147.0`).
    - Вставьте его в константу `USER_AGENT` в начале скрипта.
    - *Это критично: куки выдаются под конкретный браузер, и они должны совпадать.*

### 3. Запуск
```bash
python main.py
```

## 🛠 Использование
После запуска выберите режим:
- `all` — последовательно скачать «Избранное» и все ваши плейлисты.
- `0` — скачать только треки из раздела «Избранное».
- `ID` — ввести ID конкретного плейлиста (цифры из URL плейлиста в браузере).

## ⚠️ Ограничения
- **DRM:** Некоторые треки защищены шифрованием или недоступны в вашем регионе. Скрипт пропустит их с пометкой `[НЕТ ДОСТУПА]`.
- **Лимиты:** Не рекомендуем скачивать более 200-300 треков за один сеанс. В скрипт встроены паузы, чтобы снизить риск временной блокировки вашего IP адреса.

## 📝 Лицензия
Распространяется «как есть». Код открыт для изучения принципов работы GraphQL API и имитации сетевых запросов.
